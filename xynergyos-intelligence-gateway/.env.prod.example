# Production Environment (Cloud Run)
# These are the environment variables for the PROD deployment
# Set these in Cloud Run service configuration
# ⚠️ NEVER commit actual production secrets to git!

NODE_ENV=production
XYNERGY_ENV=prod
MOCK_MODE=false

# GCP Configuration
GCP_PROJECT_ID=xynergy-dev-1757909467
GCP_REGION=us-central1

# Firebase
FIREBASE_PROJECT_ID=xynergy-dev-1757909467

# JWT Secret (use Secret Manager: jwt-secret-prod)
# JWT_SECRET=stored-in-secret-manager

# OAuth Credentials (use Secret Manager - REAL production credentials)
# SLACK_CLIENT_ID=stored-in-secret-manager
# SLACK_CLIENT_SECRET=stored-in-secret-manager
# GMAIL_CLIENT_ID=stored-in-secret-manager
# GMAIL_CLIENT_SECRET=stored-in-secret-manager

# Redis
REDIS_HOST=10.229.184.219
REDIS_PORT=6379

# CORS (production domains only)
CORS_ORIGINS=https://app.xynergyos.com,https://xynergyos.com,https://*.xynergyos.com
